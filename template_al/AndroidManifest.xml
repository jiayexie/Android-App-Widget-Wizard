<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="aaww.template.activity"
    android:versionCode="1"
    android:versionName="1.0" >

    <uses-sdk android:minSdkVersion="3" />

    <application
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name" >
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@android:style/Theme.Translucent.NoTitleBar" > <!-- added -->
            <intent-filter>
                <!-- <action android:name="android.intent.action.MAIN" /> deleted -->
				<action android:name="android.appwidget.action.APPWIDGET_CONFIGURE" /> <!-- added -->
                <!-- <category android:name="android.intent.category.LAUNCHER" /> deleted -->
            </intent-filter>
        </activity>
        <receiver android:name="WidgetProvider">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE"/>
            </intent-filter>
            <meta-data android:resource="@xml/widget_provider" android:name="android.appwidget.provider"/>
        </receiver> <!-- added -->
    </application>

</manifest>